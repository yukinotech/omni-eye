{
  "name": "@omni-eye/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node ./scripts/clean.mjs && tsc -p tsconfig.json",
    "dev": "node ./scripts/clean.mjs && tsc -p tsconfig.json --watch",
    "lint": "echo \"No lint configured for chrome extension yet\"",
    "test": "echo \"No tests for chrome extension yet\""
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
