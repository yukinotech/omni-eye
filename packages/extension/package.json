{
  "name": "omni-eye-extension",
  "version": "0.1.0",
  "private": true,
  "files": [
    "dist/**/*",
    "scripts/**/*"
  ],
  "scripts": {
    "tsc": "tsc --project tsconfig.json",
    "build": "rimraf dist && rollup --config rollup.config.js && node ./scripts/copy-assets.js"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.0",
    "@types/chrome": "^0.1.14",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "rollup": "^2.61.1",
    "rollup-plugin-typescript2": "^0.36.0",
    "typescript": "^4.5.2"
  }
}
